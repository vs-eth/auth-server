---
  - name: restart dirsrv
    become: True
    service:
      name: "dirsrv@{{ auth_ldap_shortname }}"
      state: restarted
      enabled: yes

  - name: restart haproxy
    become: True
    service:
      name: haproxy
      state: restarted
      enabled: yes
